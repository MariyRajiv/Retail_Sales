# chatsales - Frontend

## Setup
1. `cd frontend`
2. `npm install`
3. `npm run dev` (Vite default runs on http://localhost:5173)

## Environment
- VITE_API_BASE can be set to `http://localhost:4000/api` if backend runs locally.
